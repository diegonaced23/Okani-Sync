import { internalAction } from "../_generated/server";

// Stub — implementación completa en Sprint 8
export const run = internalAction({
  args: {},
  handler: async (_ctx) => {
    console.log("sendAlerts: pendiente (Sprint 8)");
  },
});
